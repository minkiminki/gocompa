//
// primes
//
// compute prime numbers
// (not in a very efficient way)
//

module bbb;

var p: integer[1000][23][4][22][5][7];

begin
     WriteInt(DIM(p, 1));
     WriteInt(DIM(p, 2));
  WriteInt(DIM(p, 3));
  WriteInt(DIM(p, 4));
    WriteInt(DOFS(p))

end bbb.
